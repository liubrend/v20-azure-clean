project:
  name: v19-GCP-clean-teamsEnabled
  phase: init  # init | build | release. See .project/plan.md, docs/product/, CONTEXT.md
  repository: v19-GCP-clean-teamsEnabled

team:
  # Add team members here