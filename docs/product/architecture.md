# v20-Azure-clean-teamsEnabled — Architecture

> Stub. Describe the system as it is built. Keep consistent with `.project/stack.yaml`
> (the tech) and `docs/adr/` (the decisions behind it).

## Overview
<One paragraph: the pieces and how a request flows through them.>

## Components
<Backend, data layer, frontend, infra — what each owns.>

## Data flow
<Client -> API -> data store. Note any background/async paths.>

## Data model
<Core entities and their relationships.>

## Deployment
<Where it runs and how it gets there (see `infra/` and `.github/workflows/`).>
