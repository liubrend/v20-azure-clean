# Feature specs

One spec per user-facing capability, **written before code** (`.project/plan.md`,
step 1). Number them `spec-001.md`, `spec-002.md`, … Copy `spec-template.md` to start.

Each spec scenario (Given/When/Then) becomes a test (L2). No spec, no build.
